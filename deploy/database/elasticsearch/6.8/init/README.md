# Elasticsearch 6.8 smoke data

`make db-verify DB=elasticsearch@6.8` indexes and reads the `chiron-horizon-smoke` document. Elasticsearch runs in single-node development mode without authentication.
