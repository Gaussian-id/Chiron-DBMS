{
  "database": "redis",
  "name": "Redis",
  "version": "3.0.7",
  "displayVersion": "3.0.7",
  "image": "docker.cnb.cool/znb/images/redis:3.0.7-alpine",
  "platforms": ["linux/amd64"],
  "service": "database",
  "defaultPort": 6379,
  "connection": { "host": "127.0.0.1", "port": 10500, "password": "123456", "database": 0 },
  "hostPorts": { "DB_PORT": 10500 },
  "shell": ["redis-cli", "-a", "${DB_PASSWORD}"],
  "smoke": { "steps": [{ "name": "seed smoke key", "command": ["redis-cli", "-a", "${DB_PASSWORD}", "SET", "gauss-horizon:smoke", "Gauss Horizon smoke"], "expect": "OK" }, { "name": "read smoke key", "command": ["redis-cli", "-a", "${DB_PASSWORD}", "GET", "gauss-horizon:smoke"], "expect": "Gauss Horizon smoke" }] }
}
