{
  "database": "mongodb",
  "name": "MongoDB",
  "version": "5.0.5",
  "displayVersion": "5.0",
  "image": "docker.cnb.cool/znb/images/mongo:5.0.5",
  "platforms": ["linux/amd64", "linux/arm64"],
  "service": "database",
  "defaultPort": 27017,
  "connection": { "host": "127.0.0.1", "port": 10400, "username": "root", "password": "123456", "database": "gauss-horizon", "authSource": "admin" },
  "hostPorts": { "DB_PORT": 10400 },
  "shell": ["mongosh", "--quiet", "--host", "127.0.0.1", "--port", "27017", "--username", "root", "--password", "${DB_PASSWORD}", "--authenticationDatabase", "admin", "gauss-horizon"],
  "smoke": { "steps": [{ "name": "query initialized document over TCP", "command": ["mongosh", "--quiet", "--host", "127.0.0.1", "--port", "27017", "--username", "root", "--password", "${DB_PASSWORD}", "--authenticationDatabase", "admin", "gauss-horizon", "--eval", "db.gauss_horizon_smoke.findOne({_id: 1}).note"], "expect": "Gauss Horizon smoke" }] }
}
