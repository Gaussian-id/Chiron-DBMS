# Pulsar 4.2 smoke data

Pulsar standalone creates the `public/default` namespace automatically. The `verify` command publishes `Chiron Horizon smoke` to `persistent://public/default/chiron-horizon-smoke`.
