# Pulsar 4.2 smoke data

Pulsar standalone creates the `public/default` namespace automatically. The `verify` command publishes `Gauss Horizon smoke` to `persistent://public/default/gauss-horizon-smoke`.
