dependencies {
    implementation fileTree(dir: 'libs', include: ['*.jar'])
}

tasks.named('shadowJar') {
    manifest {
        attributes('Agent-Label': 'UXDB', 'Main-Class': 'com.chiron.horizon.agent.uxdb.UxdbAgent')
    }
}
