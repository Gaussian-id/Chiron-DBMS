dependencies {
    implementation 'com.gbasedbt:jdbc:3.6.5.21'
}

tasks.named('shadowJar') {
    manifest {
        attributes(
            'Agent-Label': 'GBase 8s',
            'Main-Class': 'com.chiron.horizon.agent.gbase8s.Gbase8sAgent'
        )
    }
}
