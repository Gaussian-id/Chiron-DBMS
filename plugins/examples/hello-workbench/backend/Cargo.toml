[package]
name = "chiron-horizon-example-hello"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"

[workspace]

[dependencies]
chiron-horizon-plugin-sdk = { path = "../../../sdk/rust/chiron-horizon-plugin-sdk" }
serde_json = "1"

[profile.release]
strip = true
lto = true
codegen-units = 1
