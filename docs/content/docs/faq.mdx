---
title: FAQ
description: Frequently asked questions about Gauss Horizon, including where connection data is stored and how to customize the data directory.
---

<Callout type="info">Common questions about Gauss Horizon Desktop. This page starts with data storage and the data directory, and more entries will be added over time.</Callout>

## Data and Backup

<Accordions>
  <Accordion title="Where is my connection configuration stored?">
    Gauss Horizon Desktop stores connections, saved credentials, SSH tunnel profiles, query history, and app settings in a single SQLite database file `gauss-horizon.db` inside the Gauss Horizon data directory:

    - **Windows (installer):** `C:\Users\<user>\AppData\Roaming\id.gaussian.gauss-horizon\gauss-horizon.db`
    - **Windows (portable ZIP):** `<Gauss Horizon.exe folder>\data\gauss-horizon.db`
    - **macOS:** `~/Library/Application Support/id.gaussian.gauss-horizon/gauss-horizon.db`
    - **Linux:** `~/.local/share/id.gaussian.gauss-horizon/gauss-horizon.db`

    SSH host keys are kept in `known_hosts` in the same directory, so Gauss Horizon never touches `~/.ssh`. Depending on write activity, `gauss-horizon.db-wal` and `gauss-horizon.db-shm` checkpoint files may sit next to the database.

    Older Gauss Horizon releases kept connections in `connections.json` and `secrets.json`. These files are migrated into `gauss-horizon.db` automatically on startup and the originals are renamed to `*.bak`.

    To move connections to another machine, use encrypted [Config Export/Import](/en/docs/config-export) instead of copying files. For a manual backup, quit Gauss Horizon first, then copy the whole data directory, or at least `gauss-horizon.db` together with the `-wal` and `-shm` files.
  </Accordion>
  <Accordion title="Can I keep the data next to the executable, or use a custom data directory?">
    Yes. There are two supported ways:

    - **Windows portable ZIP:** the `GAUSS_HORIZON_<version>_<arch>-portable.zip` from the release page ships with a `portable.gauss-horizon` marker file. Extract it anywhere and run it; all data is stored in the `data` folder beside `Gauss Horizon.exe`. On first launch, if the portable folder is still empty and older data exists in `AppData`, it is imported automatically once.
    - **`GAUSS_HORIZON_DATA_DIR` environment variable:** works on every platform and for both installer and portable builds, and takes the highest priority. Point it at any folder. The new directory starts empty, so migrate with Config Export/Import, or copy `gauss-horizon.db` while Gauss Horizon is not running.

    The `portable.gauss-horizon` marker only works on Windows, and it is ignored when `uninstall.exe` sits next to the executable (installed builds). There is no in-app setting for the data directory.
  </Accordion>
</Accordions>
